# 测试报告

## HTML 原型资料支持 · 2026-09-16

- 新增 .html / .htm 静态提取：标题、段落、表格、按钮、输入提示、图片替代文字；去除脚本、样式、嵌入页面与明确隐藏内容。不会发起页面外部资源请求。
- npm test：13项通过，包含HTML实体与结构、安全过滤、纯脚本拒绝、UTF-8和正文长度限制，以及真实HTTP上传、索引、检索和失败更新保留旧版。
- 浏览器更新用例改用含脚本的HTML原型，验证只索引文字、不执行脚本。构建、页面完整流程与依赖审计结果见本轮执行记录。
- 不支持原型交互、截图识别、ZIP原型包；纯脚本生成正文需先导出静态文档。外部CSS控制的显示状态不进行浏览器级判断。

## v0.1.0 · 2026-09-16

### 已通过

- npm run build：Vue / TypeScript 类型检查和 Vite 生产构建通过。
- npm test：10项测试全部通过。使用真实 PGlite 和 pgvector，不用模拟数据库替代事务行为。
- 文档解析：中文/英文分词、章节切分、UTF-8、空文件拒绝、非法扩展、文字型PDF正文与页码提取；PDF标准字体路径已适配Windows。
- 检索：空间隔离、原文摘录模式、无候选拒答、向量维度/内容校验、模拟向量服务的RRF融合及模型标签隔离。
- 生命周期：上传进入队列、新版成功原子切换、失败保留旧版；删除同时清理原文件版本、索引和关联回答。
- 引用与模型：引用ID和逐字摘录验证；DeepSeek模拟成功与无效输出降级；没有调用真实付费模型。
- HTTP：跨站Origin拒绝、跨空间文档访问404、反馈保存与删除清理。
- npm run test:ui：真实HTTP API + 隔离内存数据库 + Edge，走通创建空间、示例导入、索引、提问、点击引用、反馈、更新到v2、再次提问、删除。无页面脚本异常。
- 1440 / 1024 / 390px 页面验证；窄屏无横向溢出。已查看资料页和手机问答截图，补上手机端新建空间入口。
- npm run dev 实际启动成功，首页HTTP200、配置接口正常；停止后持久化数据库可重新打开。Windows终端强制结束可能留下短暂锁，约10秒后自动回收；不会删除数据。
- npm audit：0项已知漏洞。PDF解析器升级到修复已知高危公告的6.2.108或更高兼容版本。

### 测试产物

- artifacts/library.png：资料管理页面。
- artifacts/answer.png：问答与右侧原文定位。
- artifacts/answer-1024.png、answer-390.png：响应式问答页面。
- 所有示例为明确标注的虚构资料，UI测试不写入用户的.data数据库，不读取已有项目的文档或密钥。

### 尚未验证 / 不应声称

- 没有有效DeepSeek与embedding凭据，因此未做真实外部模型端到端调用。需要配置.env后单独验证服务的模型名、维度、限额和出站网络。
- 没有真实语料上的Recall@k、引用语义支持率或拒答准确率评估，不宣传准确率。
- 没有并发压力测试、多实例测试或恶意PDF隔离测试；第一版仅供可信资料、本机单进程使用。
- OCR、GitHub同步、多用户权限、自动冲突审核不在本版范围内。
