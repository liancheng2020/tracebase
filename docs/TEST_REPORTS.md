# 测试报告

## 问答知识库筛选 · 2026-09-16

- 默认全部知识库，可选单库，提交期间禁用筛选；回答显示实际检索范围，历史仍统一展示。
- 浏览器用例覆盖空库拒答且不引用其他库、指定库返回对应资料、恢复全部范围，以及修改筛选不改写旧回答范围。

## 知识库删除 · 2026-09-16

- 新增确认弹窗、重复提交保护和错误提示；只对隔离测试库执行删除，未删除用户资料。
- 集成验证文档、版本、片段、旧问答与跨库引用记录清理，其他知识库资料保留，重复删除返回404。
- 浏览器覆盖取消删除、确认删除、剩余知识库保留以及删除最后一个库后回到创建入口。

## 知识库聚合入口 · 2026-09-16

- 新增卡片聚合页、名称搜索、单库详情面包屑与健康异常定位；创建库后进入资料页。
- 浏览器回归新增默认聚合、双库卡片、搜索空结果、清空搜索、往返导航及 390px 无横向溢出检查。

## 统一工作台调整 · 2026-09-16

- 知识空间按库管理资料，问答及历史默认全局，知识健康汇总全部知识库；保留来源库与旧记录。
- 新增跨库检索、旧历史合并、全局反馈、引用定位、删除跨库回答及错误归属删除保护的集成用例。
- 数据库仅放宽 answers.space_id 非空约束，不删除已有文档或记录。

## 反馈提交与空闲轮询修复 · 2026-09-16

- 根因：页面每 2.5 秒无条件刷新 spaces / documents / answers，并非反馈重复提交。
- 改为仅文档 pending / indexing 且页面可见时轮询；反馈成功只同步本地反馈状态，不刷新列表。
- 反馈请求增加 10 秒超时提示，保留说明并支持手动重试，不自动重发；超时不代表服务端一定未保存，可重新查看历史回答核对。
- npm run build、npm run test:ui 通过。回归覆盖保存后 5.5 秒内无列表请求、模拟请求超时与恢复、失败重试、历史回显、健康页同步、文档索引更新及窄屏布局。

## HTML 原型资料支持 · 2026-09-16

- 新增 .html / .htm 静态提取：标题、段落、表格、按钮、输入提示、图片替代文字；去除脚本、样式、嵌入页面与明确隐藏内容。不会发起页面外部资源请求。
- npm test：13项通过，包含HTML实体与结构、安全过滤、纯脚本拒绝、UTF-8和正文长度限制，以及真实HTTP上传、索引、检索和失败更新保留旧版。
- 浏览器更新用例改用含脚本的HTML原型，验证只索引文字、不执行脚本。构建、页面完整流程与依赖审计结果见本轮执行记录。
- 不支持原型交互、截图识别、ZIP原型包；纯脚本生成正文需先导出静态文档。外部CSS控制的显示状态不进行浏览器级判断。

## v0.1.0 · 2026-09-16

### 已通过

- npm run build：Vue / TypeScript 类型检查和 Vite 生产构建通过。
- npm test：10项测试全部通过。使用真实 PGlite 和 pgvector，不用模拟数据库替代事务行为。
- 文档解析：中文/英文分词、章节切分、UTF-8、空文件拒绝、非法扩展、文字型PDF正文与页码提取；PDF标准字体路径已适配Windows。
- 检索：空间隔离、原文摘录模式、无候选拒答、向量维度/内容校验、模拟向量服务的RRF融合及模型标签隔离。
- 生命周期：上传进入队列、新版成功原子切换、失败保留旧版；删除同时清理原文件版本、索引和关联回答。
- 引用与模型：引用ID和逐字摘录验证；DeepSeek模拟成功与无效输出降级；没有调用真实付费模型。
- HTTP：跨站Origin拒绝、跨空间文档访问404、反馈保存与删除清理。
- npm run test:ui：真实HTTP API + 隔离内存数据库 + Edge，走通创建空间、示例导入、索引、提问、点击引用、反馈、更新到v2、再次提问、删除。无页面脚本异常。
- 1440 / 1024 / 390px 页面验证；窄屏无横向溢出。已查看资料页和手机问答截图，补上手机端新建空间入口。
- npm run dev 实际启动成功，首页HTTP200、配置接口正常；停止后持久化数据库可重新打开。Windows终端强制结束可能留下短暂锁，约10秒后自动回收；不会删除数据。
- npm audit：0项已知漏洞。PDF解析器升级到修复已知高危公告的6.2.108或更高兼容版本。

### 测试产物

- artifacts/library.png：资料管理页面。
- artifacts/answer.png：问答与右侧原文定位。
- artifacts/answer-1024.png、answer-390.png：响应式问答页面。
- 所有示例为明确标注的虚构资料，UI测试不写入用户的.data数据库，不读取已有项目的文档或密钥。

### 尚未验证 / 不应声称

- 没有有效DeepSeek与embedding凭据，因此未做真实外部模型端到端调用。需要配置.env后单独验证服务的模型名、维度、限额和出站网络。
- 没有真实语料上的Recall@k、引用语义支持率或拒答准确率评估，不宣传准确率。
- 没有并发压力测试、多实例测试或恶意PDF隔离测试；第一版仅供可信资料、本机单进程使用。
- OCR、GitHub同步、多用户权限、自动冲突审核不在本版范围内。

## 动态原型模板清洗与导航去噪

- 构建通过，npm test 共16项通过。新增跨行/嵌套对象/引号内括号插值、实体解码、标题过滤、静态正文保留、纯动态拒绝和导航误删保护。
- 数据库验证：警告传递至资料、检索证据及问答；更新失败保留旧有效版本提示，成功更新为静态资料后清除提示。
- 浏览器完整流程通过：上传含导航和模板表达式的HTML，问答与原文侧栏展示不完整提示，摘录无模板代码和导航噪声；包含桌面及手机验证。全部使用隔离测试资料，未修改用户知识库或调用真实模型。
- 用户已有HTML需要手动重新索引后再提问。旧回答为历史快照，不做无提示改写；模型不可用或引用校验失败的具体原因不属于本轮改动范围。

## 模型错误细分与证据不足拒答

- 构建通过，19项自动化测试与浏览器完整流程通过。新增短编号映射、逐字引用拒绝、伪造来源拒绝、拒答不得附带结论、HTTP/网络/超时/格式/截断错误分类及拒答落库验证。
- 真实DeepSeek测试仅使用两份虚构短证据：完整字段说明返回answered并通过校验；只有菜单名称返回insufficient且无回答段落。没有重放用户文档或改写历史记录。
- 异常提示不再统一显示“模型不可用”；错误分支仍安全回退摘录，并注明摘录不代表问题已回答。历史记录保留原有提示，新提问才应用新逻辑。
